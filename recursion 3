/*3. Дано натуральное число n. Найти чему равен n!.
При решении задач нельзя использовать циклы, массивы и встроенные функции */
function recursion($n) {
	if ($n <= 1) {
		return 1;
	} 
		return $n * recursion($n - 1);
}
echo recursion(5);
